# macOS
.DS_Store

# Cursor (local editor metadata)
.cursor/.DS_Store

# Environment variables / secrets
.env

# Kaleido local Chrome runtime (too large for GitHub; install locally for chart PNG export)
.kaleido_chrome/
